name: dynamic_row_grid
description: "DynamicRowGrid is a Flutter package for customizable, responsive grid layouts, ideal for flexible designs."
version: 0.0.1
homepage: https://github.com/SpyroShk/dynamic_row_grid.git

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
